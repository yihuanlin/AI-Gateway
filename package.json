{
  "name": "vercel-ai-gateway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node --watch dist/index.js",
    "build": "tsc",
    "build:netlify": "mkdir -p netlify/edge-functions && cp src/index.ts netlify/edge-functions/index.ts && perl -i -pe 's/export default app/export default (request: Request) => app.fetch(request)/' netlify/edge-functions/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.4",
    "@ai-sdk/google": "^2.0.3",
    "@ai-sdk/openai": "^2.0.8",
    "@ai-sdk/openai-compatible": "^1.0.4",
    "ai": "^5.0.8",
    "hono": "^4.6.3",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@types/node": "^22.17.1",
    "typescript": "^5.9.2"
  }
}