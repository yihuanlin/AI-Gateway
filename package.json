{
  "name": "vercel-ai-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env.local dist/index.js --port 8888",
    "build": "tsc",
    "build:netlify": "mkdir -p netlify/edge-functions && cp src/index.ts netlify/edge-functions/index.ts",
    "build:eo": "mkdir -p functions && cp src/index.ts functions/[[default]].ts",
    "watch:netlify": "fswatch -o dist/index.js | xargs -n1 -I{} netlify build",
    "dev:netlify": "trap 'jobs -p | xargs -r kill' EXIT; (bun run watch:netlify &) && netlify build && netlify dev",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.7",
    "@ai-sdk/google": "^2.0.6",
    "@ai-sdk/groq": "^2.0.10",
    "@ai-sdk/openai": "^2.0.15",
    "@ai-sdk/openai-compatible": "^1.0.7",
    "@netlify/blobs": "^10.0.8",
    "ai": "^5.0.15",
    "hono": "^4.9.2",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}